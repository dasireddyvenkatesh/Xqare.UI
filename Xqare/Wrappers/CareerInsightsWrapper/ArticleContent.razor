@using Xqare.Models.Carrer_Insights_Detail

<main class="article-body">
    @foreach (var sectionItem in Sections)
    {
        <section id="@sectionItem.Id" data-section-id="@sectionItem.Id" class="article-section">
            <MudText Typo="Typo.h2" Class="article-section__title">
                @sectionItem.Title
            </MudText>

            @foreach (var block in sectionItem.Blocks)
            {
                <ArticleContentBlock Block="block" />
            }
        </section>
    }

    <ArticleShareFooter />
    <ArticleCallToAction Model="CallToAction" />
</main>

@code {
    [Parameter] public IReadOnlyList<CareerInsightSection> Sections { get; set; } = [];
    [Parameter] public CareerInsightCallToAction CallToAction { get; set; } = new();
}